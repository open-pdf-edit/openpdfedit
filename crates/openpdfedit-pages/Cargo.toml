[package]
name = "openpdfedit-pages"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Cross-document page operations (merge, extract/split) — full-document rewrites, unlike openpdfedit-doc's incremental-save-based single-document page ops (rotate/delete/reorder/crop), since combining or subsetting object graphs across files isn't naturally incremental."

[dependencies]
lopdf.workspace = true
thiserror.workspace = true

[dev-dependencies]
openpdfedit-engine.workspace = true
