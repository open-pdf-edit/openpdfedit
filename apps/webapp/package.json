{
  "name": "openpdfedit-webapp",
  "version": "0.1.10",
  "private": true,
  "description": "OpenPdfEdit as a plain web page: the same Rust core compiled to WebAssembly, running entirely in the browser.",
  "type": "module",
  "scripts": {
    "build": "bash scripts/build.sh",
    "preview": "python3 -m http.server -d dist 8081"
  }
}
